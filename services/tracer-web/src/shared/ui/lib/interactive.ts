/** 누를 수 있는 것들이 함께 쓰는 상태이며 포커스는 `focus-ring` 유틸리티가 맡는다. */

/** `pointer-events-none`이 커서 규칙까지 겸하는 비활성 상태의 단일 표현이다. */
export const DISABLED = "disabled:opacity-50 disabled:pointer-events-none";

/** 배경과 글자색이 상태를 따라 바뀌는 요소의 전이이며 곡선은 토큰이 정한다. */
export const TRANSITION = "transition-colors duration-150 ease-out";
